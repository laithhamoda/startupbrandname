/**
 * Version of the calculation engine, stored on every tool run (`tool_runs.engine_version`)
 * so cached outputs are recomputed when a formula changes. Formulas arrive in M4.
 */
export const ENGINE_VERSION = '0.1.0';
